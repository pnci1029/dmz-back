server:
  port: 8080

jasypt:
  encryptor:
    password:

spring:
  profiles:
    include: local , secret

  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

logging:
  level:
    com:
      amazons:
        util:
          EC2MetadataUtils: error
        internal:
          InstanceMetadataServiceResourceFetcher: error

